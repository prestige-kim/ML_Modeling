# 최종 출력 안내

Lesson 4를 통과한 뒤 `bike_submission.csv`를 이 폴더에 저장한다.

필수 조건:

- 컬럼 순서: `datetime,count`
- 행 수: 6,493
- `datetime`: `data/sampleSubmission.csv`와 값과 순서가 동일
- `count`: 결측치, 무한대, 음수가 없음
- CSV 저장 시 불필요한 index 컬럼을 포함하지 않음

공식 test는 최종 feature set과 target 전략을 validation으로 확정한 뒤 한 번만 예측한다. 이 과정에서는 실제 Kaggle 업로드를 수행하지 않는다.
